<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CircularClock" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="textFillColor">@color/white</item>
        <item name="github_img">@drawable/github_white</item>
        <item name="back_img">@drawable/back_white</item>
    </style>

    <style name="FullDisplay" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>